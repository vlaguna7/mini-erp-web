// Legacy file — database access is now handled by Prisma (see prismaClient.ts)
// Kept for backwards compatibility, but no longer used.

export const initializeDatabase = async () => {
  console.log('Database managed by Prisma. Skipping legacy init.');
};

